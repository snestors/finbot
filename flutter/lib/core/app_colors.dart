import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const bgPrimary = Color(0xFF0F1117);
  static const bgCard = Color(0xFF1A1D27);
  static const bgCardLight = Color(0xFF242836);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF9CA3AF);
  static const textMuted = Color(0xFF6B7280);

  // Accents
  static const accentBlue = Color(0xFF3B82F6);
  static const accentCyan = Color(0xFF06B6D4);
  static const accentGreen = Color(0xFF22C55E);
  static const accentOrange = Color(0xFFF59E0B);
  static const accentRed = Color(0xFFEF4444);

  // UI
  static const borderSubtle = Color(0xFF2A2E3A);
  static const navActive = Color(0xFF3B82F6);
  static const navInactive = Color(0xFF6B7280);
  static const switchOn = Color(0xFF22C55E);
  static const switchOff = Color(0xFF374151);

  // Gradient for billing card
  static const billingGradientStart = Color(0xFF1E40AF);
  static const billingGradientEnd = Color(0xFF3B82F6);
}
